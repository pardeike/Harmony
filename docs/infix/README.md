# Infix implementation

Use the [Infix implementation specification](../../drafts/INFIX-NEW-IMPL-V3.md). It is the sole design reference and includes the API, behavior, compatibility requirements, test coverage, and implementation order.

The older notes have been removed to avoid conflicting instructions. The specification describes work to implement, not an already released feature.
